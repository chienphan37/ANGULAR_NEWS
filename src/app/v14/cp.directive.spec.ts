import { CpDirective } from './cp.directive';

describe('CpDirective', () => {
  it('should create an instance', () => {
    const directive = new CpDirective();
    expect(directive).toBeTruthy();
  });
});
